//! Tailscale sync (§11.6) — groups + ACL policy rendered from platform and
//! project config (`tailscale-acl.tmpl`), pushed via the Tailscale API.
//! Also provisions and rotates per-project ingress auth keys (`tag:proj-<name>`).

// TODO(phase-3): implement.
